@model List<Casgem_Protfolio.Models.Entities.TblResume>

@foreach (var item in Model)
{
    <div class="py-5 w3l-resume">
        <div class="container py-lg-5 py-3">
            <h5 class="title-small mb-2"> @item.TitleI1</h5>
            <h3 class="title-big mb-4"> @item.TitleI2 </h3>
            <p>
                @item.Description
            </p>
            <div class="mt-5">
                <!--   <a href="#download" class="btn btn-style btn-primary">Cv'mi İndir</a>-->
                <a href="@Url.Action("DownloadCV", "Portfolio", new { file = "ilayda_ozken.pdf" })" class="btn btn-style btn-primary mt-lg-5 mt-4">Cv'mi İndir</a>
            </div>
        </div>
    </div>}